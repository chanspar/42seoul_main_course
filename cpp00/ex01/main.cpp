#include "PhoneBook.hpp"

int	main(void)
{
	std::string	command;
	PhoneBook	phoneBook;
	Contact		contact;

	while(1)
	{
		std::cout << "input ADD or SEARCH or EXIT: ";
		std::cin >> command;
		if (command == "ADD")
		else if (command == "SEARCH")
		else if (command == "EXIT")

	}
	return (0);
}
